apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: daily-backup
  namespace: longhorn-system
spec:
  name: daily-backup
  cron: "0 2 * * *"   # 2am daily
  task: backup
  groups:
    - default
  retain: 7
  concurrency: 1
  labels: {}
---
apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: weekly-snapshot
  namespace: longhorn-system
spec:
  name: weekly-snapshot
  cron: "0 1 * * 0"   # 1am every Sunday
  task: snapshot
  groups:
    - snapshot
  retain: 2
  concurrency: 1
  labels: {}
