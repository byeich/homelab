---
# Pin a version like "v1.32.3+k3s1" or leave empty for latest
k3s_version: ""

# Shared cluster secret — override this with an Ansible vault variable
k3s_token: "changeme"

# Default port for k3s API. allow communication between servers and agents
k3s_api_port: 6443
