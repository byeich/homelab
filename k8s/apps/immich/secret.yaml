# kubectl create secret generic immich-secret -n immich \
#   --from-literal=db-password='your-db-password'
#
# This file is a placeholder so ArgoCD knows the secret is expected.
apiVersion: v1
kind: Secret
metadata:
  name: immich-secret
  namespace: immich
type: Opaque
